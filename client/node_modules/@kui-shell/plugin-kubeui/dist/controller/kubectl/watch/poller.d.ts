import { Arguments } from '@kui-shell/core';
import { KubeOptions } from '../options';
export default function pollUntil404(apiVersion: string, kind: string, name: string, namespace: string, offline: (rowKey: string) => void, { REPL }: Arguments<KubeOptions>): {
    abort: () => void;
};
