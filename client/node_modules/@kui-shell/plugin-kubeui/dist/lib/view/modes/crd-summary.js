"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@kui-shell/core");
const resource_1 = require("../../model/resource");
const show_crd_managed_resources_1 = require("./show-crd-managed-resources");
const strings = core_1.i18n('plugin-kubeui');
function content(tab, crd) {
    return __awaiter(this, void 0, void 0, function* () {
        console.error('!!!!! ', `${show_crd_managed_resources_1.command(tab, crd)} -o custom-columns=KIND:.kind`, yield tab.REPL.qexec(`${show_crd_managed_resources_1.command(tab, crd)} -o custom-columns=NAME:.metadata.name`));
        const [{ safeDump }, { body: resources }] = yield Promise.all([
            Promise.resolve().then(() => require('js-yaml')),
            tab.REPL.qexec(`${show_crd_managed_resources_1.command(tab, crd)} -o custom-columns=NAME:.metadata.name`)
        ]);
        const { group, version, scope } = crd.spec;
        const kind = crd.spec.names.kind;
        const resourceCount = resources.length;
        return {
            content: safeDump({ scope, group, version, kind, 'resource count': resourceCount }),
            contentType: 'yaml'
        };
    });
}
const mode = {
    when: resource_1.isCustomResourceDefinition,
    mode: {
        mode: 'summary',
        label: strings('summary'),
        content,
        priority: 10
    }
};
exports.default = mode;
//# sourceMappingURL=crd-summary.js.map