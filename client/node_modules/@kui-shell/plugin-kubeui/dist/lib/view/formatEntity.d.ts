import { Tab } from '@kui-shell/core';
export declare const formatContextAttr: (context: string, extraCSS?: string) => {
    key: string;
    value: string;
    outerCSS: string;
}[];
export declare const formatEntity: (tab: Tab, parsedOptions: Record<string, any>, context?: string) => (kubeEntity: any) => Promise<any>;
