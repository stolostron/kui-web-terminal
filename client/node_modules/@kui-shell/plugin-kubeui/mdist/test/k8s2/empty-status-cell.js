import { dirname, join } from 'path';
import { Common, CLI, ReplExpect } from '@kui-shell/test';
const ROOT = dirname(require.resolve('@kui-shell/plugin-kubeui/tests/package.json'));
const input = join(ROOT, 'data/k8s/empty-status-cell.txt');
Common.localDescribe('k8s table with empty status cell', function () {
    before(Common.before(this));
    after(Common.after(this));
    xit('should format a table with an empty status cell', () => CLI.command(`kdebug "${input}"`, this.app)
        .then(ReplExpect.okWith('reviews-v3-rollout'))
        .catch(Common.oops(this)));
});
//# sourceMappingURL=empty-status-cell.js.map