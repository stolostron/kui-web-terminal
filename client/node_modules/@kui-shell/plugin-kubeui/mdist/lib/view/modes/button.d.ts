import { Tab } from '@kui-shell/core';
import { KubeResource } from '../../model/resource';
interface BaseInfo {
    mode: string;
    label?: string;
    fontawesome?: string;
    balloon?: string;
}
export declare const renderButton: (mode: string) => (tab: Tab, resource: KubeResource<import("../../model/resource").KubeStatus>) => string;
declare const makeButton: (overrides: BaseInfo) => {
    mode: string;
    label: string;
    kind: "drilldown";
    command: (tab: Tab, resource: KubeResource<import("../../model/resource").KubeStatus>) => string;
    confirm: boolean;
};
export default makeButton;
