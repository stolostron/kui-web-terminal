import { Tab, Table } from '@kui-shell/core';
import { KubeResource } from '@kui-shell/plugin-kubeui';
export declare function formatAsTable(raw: string, metadata: {
    name: string;
    namespace?: string;
}): Promise<Table>;
export default function renderLogs(tab: Tab, resource: KubeResource): Promise<Table>;
