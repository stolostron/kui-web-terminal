import { KubeResource } from '@kui-shell/plugin-kubeui';
import renderLogs from '../renderers/table';
declare function isLogs(resource: KubeResource): boolean;
declare const _default: {
    when: typeof isLogs;
    mode: {
        mode: string;
        label: string;
        content: typeof renderLogs;
        priority: number;
    };
};
export default _default;
