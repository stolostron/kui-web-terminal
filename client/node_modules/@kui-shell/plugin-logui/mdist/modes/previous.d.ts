import { Tab, Table } from '@kui-shell/core';
import { KubeResource } from '@kui-shell/plugin-kubeui';
declare function isLogs(resource: KubeResource): boolean;
declare function renderPrevious(tab: Tab, resource: KubeResource): Promise<Table>;
declare const _default: {
    when: typeof isLogs;
    mode: {
        mode: string;
        label: string;
        content: typeof renderPrevious;
        priority: number;
    };
};
export default _default;
