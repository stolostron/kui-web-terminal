import { Tab } from '../tab';
import { Block } from '../models/block';
import { Table, Row } from '../models/table';
/** RowFormatOptions helps FormatOneRowResult render the `Row` */
export interface RowFormatOptions {
    excludePackageName?: boolean;
    useRepeatingEffect?: boolean;
    usePip?: boolean;
}
/**
 * Format one row in the table
 *
 */
export declare const formatOneRowResult: (tab: Tab, options?: RowFormatOptions) => (entity: Row) => HTMLElement;
/**
 * Format the table view
 *
 */
export declare const formatTable: (tab: Tab, response: Table<Row>, resultDom: HTMLElement, options?: {
    block?: Block;
    usePip?: boolean;
}) => void;
