import { Tab } from '@kui-shell/core';
import { LogEntryResource } from '../models/resource';
declare function hasStackTrace(resource: LogEntryResource): boolean;
declare function renderStackTrace(tab: Tab, resource: LogEntryResource): HTMLDivElement;
declare const _default: {
    when: typeof hasStackTrace;
    mode: {
        mode: string;
        label: string;
        content: typeof renderStackTrace;
    };
};
export default _default;
