import { Tab } from '@kui-shell/core';
import { KubeResource } from '@kui-shell/plugin-kubeui';
declare function hasLogs(resource: KubeResource): boolean;
declare const _default: {
    when: typeof hasLogs;
    mode: {
        mode: string;
        label: string;
        command: (tab: Tab, resource: KubeResource<import("@kui-shell/plugin-kubeui").KubeStatus>) => string;
        kind: "drilldown";
    };
};
export default _default;
