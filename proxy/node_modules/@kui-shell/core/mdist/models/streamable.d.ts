import { SimpleEntity, MixedResponse } from './entity';
import { Table } from '../webapp/models/table';
import { CustomSpec } from '../webapp/views/sidecar';
export declare type Streamable = SimpleEntity | Table | CustomSpec | MixedResponse;
export default Streamable;
export declare type Stream = (response: Streamable, killLine?: boolean) => Promise<void>;
export declare type StreamableFactory = () => Promise<Stream>;
