import { Tab } from '../webapp/cli';
export declare class WatchableJob {
    private tab;
    private handler;
    private timeout;
    private _id;
    get id(): number;
    constructor(tab: Tab, handler: TimerHandler, timeout: number);
    private startWatching;
    start(): void;
    private stopWatching;
    abort(): void;
}
