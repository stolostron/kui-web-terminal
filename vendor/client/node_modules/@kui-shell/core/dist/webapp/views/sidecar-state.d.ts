import { Tab } from '../tab';
export declare enum SidecarState {
    NotShown = 0,
    Minimized = 1,
    Open = 2,
    FullScreen = 3
}
export declare const getSidecarState: (tab: Tab) => SidecarState;
