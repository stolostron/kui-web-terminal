import { Tab } from '../webapp/tab';
/** Initialize the session for a given `Tab` */
export declare type SessionInitializer = (tab: Tab) => void | Promise<void>;
/** Register one SessionInitializer */
export declare function registerSessionInitializer(init: SessionInitializer): void;
/** Invoke each registered session initializer on the given tab */
export default function MapTo(tab: Tab): Promise<void>;
