export interface Abortable {
    abort(): void;
}
export interface FlowControllable {
    xon(): void;
    xoff(): void;
    write(data: string): void;
}
/** in the future, a WatchableJob may be more than Abortable, e.g. Suspendable */
export declare type WatchableJob = Abortable;
