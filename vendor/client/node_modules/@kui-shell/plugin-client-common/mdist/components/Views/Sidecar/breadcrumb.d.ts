import { Breadcrumb } from '@kui-shell/core';
declare type TopNavBreadcrumb = Breadcrumb & {
    deemphasize?: boolean;
    isCurrentPage?: boolean;
    className?: string;
};
export default TopNavBreadcrumb;
