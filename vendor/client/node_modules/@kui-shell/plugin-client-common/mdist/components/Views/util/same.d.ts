import { ParsedOptions } from '@kui-shell/core';
import { BaseHistoryEntry } from './CircularBuffer';
/** Confirm equality of two commands, by comparing the parsed argvNoOptions and ParsedOptions */
export default function sameCommand(expectedArgvNoOptions: string[], expectedParsedOptions: ParsedOptions, currentCwd: string): (entry: BaseHistoryEntry) => boolean;
