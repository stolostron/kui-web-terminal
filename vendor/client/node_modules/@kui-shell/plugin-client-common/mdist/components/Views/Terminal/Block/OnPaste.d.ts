import { Tab as KuiTab } from '@kui-shell/core';
export declare const doPaste: (text: string, tab: KuiTab, prompt: HTMLInputElement) => Promise<void>;
/**
 * User has requested that we paste something from the
 * clipboard.
 *
 * @return a Promise of when we will be done.
 *
 */
export declare function onPasteAsync(event: ClipboardEvent, tab: KuiTab, prompt: HTMLInputElement): Promise<void>;
/**
 * User has requested that we paste something from the clipboard
 *
 */
export default function onPaste(event: ClipboardEvent, tab: KuiTab, prompt: HTMLInputElement): void;
