import * as React from 'react';
import { Tab as KuiTab, Button, ResourceWithMetadata, MultiModalResponse, ParsedOptions } from '@kui-shell/core';
interface Props {
    tab: KuiTab;
    button: Button;
    response: MultiModalResponse;
    args: {
        argvNoOptions: string[];
        parsedOptions: ParsedOptions;
    };
}
export default class ToolbarButton<T extends ResourceWithMetadata = ResourceWithMetadata> extends React.PureComponent<Props> {
    private getCommand;
    private buttonOnclick;
    render(): JSX.Element;
}
export {};
