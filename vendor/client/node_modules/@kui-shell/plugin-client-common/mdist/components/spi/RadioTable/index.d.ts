import * as React from 'react';
import Props from './model';
export interface State {
    page: number;
    pageSize: number;
    offset: number;
    selectedIdx: number;
}
/** @return the row slice for the current page */
export declare function slice(props: Props & State): (import("@kui-shell/core").RadioTableRow & import("@kui-shell/core/mdist/models/RadioTable").Selectable)[];
export default class RadioTableSpi extends React.PureComponent<Props, State> {
    constructor(props: Props);
    /**
     * If the table is going to be paginated, bring the selected row to
     * the top.
     *
     */
    private swapSelectedToTop;
    private onChange;
    private numRows;
    private isPaginated;
    private topToolbar;
    private bottomToolbar;
    private carbon;
    private broker;
    render(): JSX.Element;
}
