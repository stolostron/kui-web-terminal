import { ReactNode } from 'react';
interface Props {
    id?: string;
    title?: string;
    type?: 'ok' | 'error';
    className?: string;
    children: ReactNode;
    onClick?: () => void;
}
export default Props;
