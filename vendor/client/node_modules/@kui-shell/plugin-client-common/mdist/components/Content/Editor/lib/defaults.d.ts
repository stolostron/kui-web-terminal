import { editor } from 'monaco-editor';
export interface Options {
    readOnly?: boolean;
    simple?: boolean;
}
declare const _default: (options: Options) => editor.IEditorConstructionOptions;
export default _default;
