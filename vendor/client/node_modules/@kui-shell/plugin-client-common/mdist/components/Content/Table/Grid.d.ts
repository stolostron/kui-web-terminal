import { Tab, REPL, Table as KuiTable } from '@kui-shell/core';
import * as React from 'react';
import { NamedDataTableRow } from './kui2carbon';
/** parameters to Grid component */
export declare type Props<T extends KuiTable = KuiTable> = {
    tab: Tab;
    repl: REPL;
    response: T;
    visibleRows: NamedDataTableRow[];
    justUpdated: Record<string, boolean>;
};
export declare const findGridableColumn: (response: KuiTable<import("@kui-shell/core").Row>) => number;
/**
 * A Grid table
 *
 */
export default class Grid<P extends Props> extends React.PureComponent<P> {
    render(): JSX.Element;
}
