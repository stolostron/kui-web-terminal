/// <reference types="react" />
import { Cell as KuiCell, Row as KuiRow, Tab, REPL } from '@kui-shell/core';
import { DataTableCell } from 'carbon-components-react';
/**
 * Generate an onclick handler for a cell
 *
 */
export declare function onClickForCell(row: KuiRow, tab: Tab, repl: REPL, cell?: KuiCell, selectRow?: () => void): () => any;
/**
 * Render a TableCell part
 *
 */
export default function renderCell(kuiRow: KuiRow, justUpdated: boolean, tab: Tab, repl: REPL): (cell: DataTableCell<any, import("carbon-components-react").DataTableHeader<string>>, cidx: number) => JSX.Element;
