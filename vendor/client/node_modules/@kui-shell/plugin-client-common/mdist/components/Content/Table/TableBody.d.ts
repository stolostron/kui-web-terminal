/// <reference types="react" />
import { Row as KuiRow, Tab, REPL } from '@kui-shell/core';
import { DataTableCustomRenderProps } from 'carbon-components-react';
import { NamedDataTableRow } from './kui2carbon';
/**
 * Render the TableBody part
 *
 * @param offset offset into the kuiBody model
 *
 */
export default function renderBody(kuiBody: KuiRow[], justUpdated: Record<string, boolean>, // rowKey index
renderOpts: DataTableCustomRenderProps<NamedDataTableRow>, tab: Tab, repl: REPL, offset: number): JSX.Element;
