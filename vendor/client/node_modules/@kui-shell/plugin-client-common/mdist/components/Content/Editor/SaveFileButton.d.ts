import { editor as Monaco } from 'monaco-editor';
import { Button, REPL } from '@kui-shell/core';
import { File } from '@kui-shell/plugin-bash-like/fs';
/**
 * Offer a Save button
 *
 */
export default function SaveFileButton(editor: Monaco.ICodeEditor, repl: REPL, response: File, onSave: (success: boolean) => void): Button;
