import * as React from 'react';
import { Props as BreadcrumbProps } from '../../spi/Breadcrumb';
import 'carbon-components/scss/components/pagination/_pagination.scss';
export declare type Props = Partial<GridProps> & Partial<PaginationProps> & Partial<BreadcrumbProps> & {
    framed?: boolean;
    className?: string;
};
interface GridProps {
    gridableColumn: number;
    asGrid?: boolean;
    setAsGrid: (asGrid: boolean) => void;
}
interface PaginationProps {
    paginate: boolean;
    page: number;
    totalItems: number;
    pageSize: number;
    setPage: (page: number) => void;
}
export default class Toolbar extends React.PureComponent<Props> {
    private hasGridButtons;
    private hasButtons;
    private gridController;
    private previousPage;
    private nextPage;
    private paginationController;
    private breadcrumbs;
    private buttons;
    /** so that buttons are flush-left and pagination is flush-right */
    private filler;
    render(): JSX.Element;
}
export {};
