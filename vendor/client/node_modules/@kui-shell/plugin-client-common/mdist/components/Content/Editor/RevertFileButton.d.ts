import { editor as Monaco } from 'monaco-editor';
import { File } from '@kui-shell/plugin-bash-like/fs';
import { Button, REPL } from '@kui-shell/core';
/**
 * Offer a Revert button
 *
 */
export default function RevertFileButton(editor: Monaco.ICodeEditor, repl: REPL, response: File, onRevert: (success: boolean, data?: string) => void): Button;
