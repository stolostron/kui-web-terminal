import { Arguments, ParsedOptions } from '@kui-shell/core';
interface Options extends ParsedOptions {
    debug: boolean;
}
/**
 * This plugin introduces the /split command
 *
 */
export default function split(args?: Arguments<Options>): Promise<unknown>;
export {};
