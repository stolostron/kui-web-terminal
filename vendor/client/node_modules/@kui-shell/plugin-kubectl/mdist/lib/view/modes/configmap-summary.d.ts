import { Tab, ModeRegistration } from '@kui-shell/core';
import { KubeResource, ConfigMap } from '../../model/resource';
export declare function getDefaultSummaryMap(tab: Tab, resource: KubeResource, args: {
    argvNoOptions: string[];
}): Promise<Record<string, string | number | import("./table-to-map").LabelValue>>;
/**
 * Add a Events mode button to the given modes model, if called for by
 * the given resource.
 *
 */
declare const mode: ModeRegistration<ConfigMap>;
export default mode;
