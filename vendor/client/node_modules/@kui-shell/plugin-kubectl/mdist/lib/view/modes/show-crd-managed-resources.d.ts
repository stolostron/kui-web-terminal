import { Tab, ModeRegistration } from '@kui-shell/core';
import { CustomResourceDefinition } from '../../model/resource';
/**
 * Extract the events
 *
 */
export declare function command(tab: Tab, crd: CustomResourceDefinition, args: {
    argvNoOptions: string[];
}): Promise<string>;
/**
 * Add a Events mode button to the given modes model, if called for by
 * the given resource.
 *
 */
declare const mode: ModeRegistration<CustomResourceDefinition>;
export default mode;
