import { CommandOptions } from '@kui-shell/core';
export declare function flags(booleans?: string[]): CommandOptions;
declare const defaultFlags: CommandOptions;
export declare const crudFlags: CommandOptions & {
    alwaysViewIn: string;
};
export default defaultFlags;
