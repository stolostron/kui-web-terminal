import { HelmRelease, isHelmRelease } from '../models/release';
declare const _default: {
    when: typeof isHelmRelease;
    mode: {
        mode: string;
        label: string;
        content: (_: any, resource: HelmRelease) => {
            contentFrom: string;
            contentType: "text/markdown";
        };
    };
};
export default _default;
