import { Arguments, ExecOptions, Registrar, NavResponse } from '@kui-shell/core';
import { KubeOptions } from '@kui-shell/plugin-kubectl';
/**
 * Format the output of a helm status command
 *
 */
export declare const format: (name: string, args: Arguments<KubeOptions>, response: string, execOptions: ExecOptions) => Promise<NavResponse | import("@kui-shell/core").Table<import("@kui-shell/core").Row>[]>;
declare const _default: (registrar: Registrar) => void;
export default _default;
