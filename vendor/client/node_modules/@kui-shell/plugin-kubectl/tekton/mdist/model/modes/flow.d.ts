import { ResourceWithMetadata, Mode } from '@kui-shell/core';
import { KubeResource } from '@kui-shell/plugin-kubectl';
export declare type ResponseObject = KubeResource | (ResourceWithMetadata & {
    isFromFlowCommand?: boolean;
    model: KubeResource[];
    resource: KubeResource;
});
/**
 * The sidecar mode for the tekton flow visualization
 *
 */
declare const flowMode: Mode;
export default flowMode;
