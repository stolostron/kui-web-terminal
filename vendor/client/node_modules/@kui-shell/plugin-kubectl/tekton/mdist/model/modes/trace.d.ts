import { KubeResource } from '@kui-shell/plugin-kubectl';
import { Badge, Mode, Tab } from '@kui-shell/core';
import { ActivationLikeFull as ActivationLike } from '@kui-shell/plugin-wskflow';
import { Pipeline, PipelineRun } from '../resource';
interface RenderOpts {
    noPip?: boolean;
    noCrop?: boolean;
    showStart?: boolean;
    showTimeline?: boolean;
}
/**
 * Render a trace view in the given container
 *
 */
export declare const render: (tab: Tab, activations: ActivationLike[], container: Element, opts?: RenderOpts) => void;
export declare const traceView: (tab: Tab, run: PipelineRun, pipeline: Pipeline, jsons: KubeResource<import("../../../../mdist").KubeStatus, import("../../../../mdist/lib/model/resource").KubeMetadata>[]) => {
    type: string;
    isEntity: boolean;
    name: string;
    packageName: string;
    prettyType: string;
    duration: number;
    badges: Badge[];
    content: HTMLDivElement;
};
/**
 * Sidecar mode for a pipeline run trace view
 *
 */
declare const traceMode: Mode;
export default traceMode;
