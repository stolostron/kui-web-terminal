/// <reference types="react" />
import { InputProvider, InputProviderProps, InputProviderState } from '@kui-shell/plugin-client-common';
import '../../web/scss/index.scss';
interface SearchState extends InputProviderState {
    inputText: string;
    isSearch: boolean;
}
export default class CustomSearchInput extends InputProvider<SearchState> {
    constructor(props: InputProviderProps);
    toggleIsSearchState(inputText: any): void;
    handleInputTextChange: (e: any) => void;
    handleSearchTextChange: (currentQuery: any) => void;
    onKeyPress: (e: any) => Promise<void>;
    renderSearchComponents(): JSX.Element;
    protected input(): JSX.Element;
}
export {};
