import { EditableSpec } from '@kui-shell/core';
export declare function editSpec(cmd: string, resource?: any, data?: any): EditableSpec;
export declare const yamlTab: (resource: any, data?: any, cmd?: any) => {
    mode: string;
    label: string;
    order: number;
    content: any;
    contentType: string;
    spec: EditableSpec;
};
