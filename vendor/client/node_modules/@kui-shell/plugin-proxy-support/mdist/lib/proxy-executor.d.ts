import { Arguments, Evaluator, ExecOptions, KResponse, ParsedOptions, ReplEval } from '@kui-shell/core';
/**
 * A repl.exec implementation that proxies to the packages/proxy container
 *
 */
declare class ProxyEvaluator implements ReplEval {
    name: string;
    /**
     * The proxy server configuration.
     *
     */
    private readonly proxyServerConfig;
    apply<T extends KResponse, O extends ParsedOptions>(command: string, execOptions: ExecOptions, evaluator: Evaluator<T, O>, args: Arguments<O>): Promise<T>;
}
export default ProxyEvaluator;
