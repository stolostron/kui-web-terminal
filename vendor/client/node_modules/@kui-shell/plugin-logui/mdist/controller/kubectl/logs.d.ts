import { Arguments, Registrar } from '@kui-shell/core';
import { KubeOptions } from '@kui-shell/plugin-kubeui';
interface LogOptions extends KubeOptions {
    f: string;
    follow: string;
    previous: boolean;
    tail: number;
}
export declare function doLogs(args: Arguments<LogOptions>): Promise<any>;
declare const _default: (registrar: Registrar) => void;
export default _default;
