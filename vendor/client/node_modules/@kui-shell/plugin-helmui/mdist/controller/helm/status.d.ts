import { ExecOptions, MixedResponse, Registrar } from '@kui-shell/core';
import { KubeOptions } from '@kui-shell/plugin-kubeui';
export declare const format: (options: KubeOptions, response: string, execOptions: ExecOptions) => Promise<MixedResponse>;
declare const _default: (registrar: Registrar) => void;
export default _default;
