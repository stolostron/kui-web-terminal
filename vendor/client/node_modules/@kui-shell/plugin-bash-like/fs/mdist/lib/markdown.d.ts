import { Tab } from '@kui-shell/core';
interface Markdown {
    title: HTMLElement;
    body: HTMLElement;
}
declare const _default: (tab: Tab, suffix: string, source: string, fullpath: string) => Promise<Markdown>;
/**
 * Render a markdown file as HTML
 *
 */
export default _default;
