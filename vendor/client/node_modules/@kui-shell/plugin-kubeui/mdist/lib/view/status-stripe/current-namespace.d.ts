import { Tab, StatusStripeController, StatusTextWithIcon } from '@kui-shell/core';
declare function listener(tab: Tab, controller: StatusStripeController, fragment: StatusTextWithIcon): Promise<void>;
export default function (): {
    fragment: {
        id: string;
        icon: string;
        iconIsNarrow: boolean;
        text: HTMLDivElement;
        onclick: {
            text: string;
        };
    };
    listener: typeof listener;
};
export {};
