import { ResourceWithMetadata } from '@kui-shell/core';
export interface RawOutput {
    code: number;
    stdout: string;
    stderr: string;
    wasSentToPty: boolean;
}
export declare type RawResponse = ResourceWithMetadata<RawOutput>;
export default RawResponse;
