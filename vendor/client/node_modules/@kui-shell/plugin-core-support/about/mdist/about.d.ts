import { Registrar, NavResponse } from '@kui-shell/core';
declare const _default: (commandTree: Registrar) => Promise<NavResponse>;
/**
 * Here we install the command handlers for /version and /about
 *
 */
export default _default;
