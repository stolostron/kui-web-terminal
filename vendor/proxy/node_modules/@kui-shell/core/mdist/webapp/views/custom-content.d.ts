/**
 * Show custom content in the sidecar
 *
 */
import { CustomSpec } from './sidecar-core';
import { MetadataBearing, Entity } from '../../models/entity';
export declare function isCustomSpec(entity: MetadataBearing | CustomSpec | Entity): entity is CustomSpec;
