import { ResourceWithMetadata } from '@kui-shell/core';
import { KubeResourceWithSummary } from '@kui-shell/plugin-kubectl';
export interface HelmRelease extends KubeResourceWithSummary {
    apiVersion: any;
    kind: any;
    isSimulacrum: true;
}
export declare function isHelmRelease(resource: ResourceWithMetadata): resource is HelmRelease;
