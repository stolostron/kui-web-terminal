import { REPL as REPLType, Registrar } from '@kui-shell/core';
import { KubeContext } from '../../lib/model/resource';
/**
 * @return a `KubeContext` representing the current context
 *
 */
export declare function getCurrentContext({ REPL }: {
    REPL: REPLType;
}): Promise<KubeContext>;
export declare function getCurrentContextName({ REPL }: {
    REPL: REPLType;
}): Promise<string>;
/** Extract the namespace from the current context */
export declare function getCurrentDefaultNamespace({ REPL }: {
    REPL: REPLType;
}): Promise<string>;
declare const _default: (commandTree: Registrar) => void;
/**
 * Register the commands
 *
 */
export default _default;
