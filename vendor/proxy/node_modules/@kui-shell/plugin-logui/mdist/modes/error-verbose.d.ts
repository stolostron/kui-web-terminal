import { Tab } from '@kui-shell/core';
import { LogEntryResource } from '../models/resource';
declare function hasError(resource: LogEntryResource): boolean;
declare function renderError(tab: Tab, resource: LogEntryResource): any;
declare const _default: {
    when: typeof hasError;
    mode: {
        mode: string;
        label: string;
        content: typeof renderError;
        order: number;
    };
};
export default _default;
