import { Tab, ExecOptions, ParsedOptions } from '@kui-shell/core';
import '../../web/css/static/xterm.css';
interface Options extends ParsedOptions {
    o?: string;
    out?: string;
    output?: string;
}
/**
 *
 *
 */
export declare const doExec: (tab: Tab, cmdline: string, argvNoOptions: string[], parsedOptions: Options, execOptions: ExecOptions) => Promise<unknown>;
export {};
