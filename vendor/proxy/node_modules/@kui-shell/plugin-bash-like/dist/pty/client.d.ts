import { Tab, ExecOptions, ParsedOptions } from '@kui-shell/core';
interface Options extends ParsedOptions {
    o?: string;
    out?: string;
    output?: string;
}
export declare const doExec: (tab: Tab, block: HTMLElement, cmdline: string, argvNoOptions: string[], parsedOptions: Options, execOptions: ExecOptions) => Promise<unknown>;
export {};
