import { Button } from '@kui-shell/core';
import { EditorResource, EditorResponse, EditorState } from './response';
declare type ModeFunction = (state: EditorState) => Button<EditorResource>;
export declare const respondToRepl: (extraModes?: ModeFunction[]) => (response: EditorResponse) => import("@kui-shell/core").MultiModalResponse<EditorResource>;
export default respondToRepl;
