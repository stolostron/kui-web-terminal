import { ExecOptions, ParsedOptions, Registrar, Tab } from '@kui-shell/core';
import EditorEntity from '../entity';
interface Options extends ParsedOptions {
    language?: string;
}
/**
 * Open editor to a given entity, passed programmatically
 *
 */
export declare const edit: (tab: Tab, entity: EditorEntity, options: Options, execOptions: ExecOptions) => Promise<import("@kui-shell/core").MultiModalResponse<import("../response").EditorResource>>;
declare const _default: (commandTree: Registrar) => Promise<void>;
export default _default;
