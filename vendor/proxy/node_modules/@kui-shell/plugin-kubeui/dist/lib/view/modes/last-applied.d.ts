import { Tab } from '@kui-shell/core';
import { KubeResource } from '../../model/resource';
declare function hasLastApplied(resource: KubeResource): boolean;
declare const _default: {
    when: typeof hasLastApplied;
    mode: {
        mode: string;
        label: string;
        content: (tab: Tab, resource: KubeResource<import("../../model/resource").KubeStatus>) => Promise<{
            content: any;
            contentType: string;
        }>;
    };
};
export default _default;
