import { Tab, Row, Table } from '@kui-shell/core';
import { KubeResource } from '../../model/resource';
export declare const renderConditions: (tab: Tab, resource: KubeResource<import("../../model/resource").KubeStatus>) => Promise<Table<Row>>;
declare const _default: {
    when: (resource: KubeResource<import("../../model/resource").KubeStatus>) => boolean;
    mode: {
        mode: string;
        label: string;
        content: (tab: Tab, resource: KubeResource<import("../../model/resource").KubeStatus>) => Promise<Table<Row>>;
    };
};
export default _default;
