import { hasRawData } from '../../model/resource';
const yamlMode = {
    when: hasRawData,
    mode: {
        mode: 'raw',
        label: 'YAML',
        content: (_, resource) => ({
            content: resource.data,
            contentType: 'yaml'
        }),
        order: 999
    }
};
export default yamlMode;
//# sourceMappingURL=yaml.js.map