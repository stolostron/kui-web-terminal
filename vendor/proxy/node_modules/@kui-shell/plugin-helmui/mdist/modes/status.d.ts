import { HelmRelease, isHelmRelease } from '../models/release';
declare const _default: {
    when: typeof isHelmRelease;
    mode: {
        mode: string;
        label: string;
        kind: "drilldown";
        command: (_: any, resource: HelmRelease) => string;
    };
};
export default _default;
